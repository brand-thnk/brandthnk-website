# Local Netlify folder
.netlify

# Netlify deploy artifacts (never commit or keep in vault)
deploy-*.zip

# Environment variables
.env
.env.local
.env.production

# OAuth credentials
*credentials*
*token*
*secret*
