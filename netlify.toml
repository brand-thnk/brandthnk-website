[build]
  functions = "netlify/functions"
  included_files = ["newsletter/**"]

[functions]
  node_bundler = "esbuild"

# Newsletter scheduled function runs daily at 8am EST
[functions."newsletter-send"]
  schedule = "0 13 * * *"
